/** Как часто спрашивать бота о проверке, пока она идёт (сам бот ждёт у сервиса до 30 с). */
export const CHECK_POLL_MS = 1500;
/** Сколько секунд бот держит запрос у сервиса (long-poll), пока проверка не закончилась. */
export const CHECK_WAIT_SEC = 25;
